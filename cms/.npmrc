# Convierte el mínimo de `engines.node` en una barrera de instalación y no solo en documentación.
# El hook de edición corre con `node` puro y depende del type-stripping del runtime: sin este flag,
# una máquina por debajo del piso instala igual y el guardarraíl degrada en abierto, sin aviso.
engine-strict=true
