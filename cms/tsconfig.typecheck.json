{
	// Config del gate de tipos del Studio (step `Typecheck Sanity Studio` del job studio-build). Toda la
	// configuración vive en la base, que es la que además lee el editor: acá solo se declara el archivo
	// como punto de entrada de `tsc -p`, para que gate y editor no puedan divergir.
	"extends": "./tsconfig.json"
}
