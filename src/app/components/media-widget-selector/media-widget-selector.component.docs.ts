import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de MediaWidgetSelector. La consume su `meta` y toda story que la referencie. */
export const mediaWidgetSelectorDocs: StoryDocsEntry = { title: 'Componentes V3/MediaWidgetSelector' };
