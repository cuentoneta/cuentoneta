import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de SpotifyPodcastEpisode. La consume su `meta` y toda story que la referencie. */
export const spotifyPodcastEpisodeDocs: StoryDocsEntry = { title: 'Widgets/SpotifyPodcastEpisode' };
