import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de SpaceRecording. La consume su `meta` y toda story que la referencie. */
export const spaceRecordingDocs: StoryDocsEntry = { title: 'Widgets/SpaceRecording' };
