import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de ButtonGroup. La consume su `meta` y toda story que la referencie. */
export const buttonGroupDocs: StoryDocsEntry = { title: 'Componentes V3/ButtonGroup' };
