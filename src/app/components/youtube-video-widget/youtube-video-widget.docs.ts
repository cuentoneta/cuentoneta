import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de YoutubeVideo. La consume su `meta` y toda story que la referencie. */
export const youtubeVideoDocs: StoryDocsEntry = { title: 'Widgets/YoutubeVideo' };
