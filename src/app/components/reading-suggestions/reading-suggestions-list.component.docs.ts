import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de ReadingSuggestionsList. La consume su `meta` y toda story que la referencie. */
export const readingSuggestionsListDocs: StoryDocsEntry = { title: 'Componentes V3/ReadingSuggestionsList' };
