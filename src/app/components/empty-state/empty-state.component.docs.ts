import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de EmptyState. La consume su `meta` y toda story que la referencie. */
export const emptyStateDocs: StoryDocsEntry = { title: 'Componentes V3/EmptyState' };
