import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de LiteraryWorkHeroHeader. La consume su `meta` y toda story que la referencie. */
export const literaryWorkHeroHeaderDocs: StoryDocsEntry = { title: 'Componentes V3/LiteraryWorkHeroHeader' };
