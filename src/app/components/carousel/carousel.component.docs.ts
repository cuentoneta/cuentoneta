import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de Carousel. La consume su `meta` y toda story que la referencie. */
export const carouselDocs: StoryDocsEntry = { title: 'Componentes V3/Carousel' };
