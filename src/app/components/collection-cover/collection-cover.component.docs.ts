import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de CollectionCover. La consume su `meta` y toda story que la referencie. */
export const collectionCoverDocs: StoryDocsEntry = { title: 'Componentes V3/CollectionCover' };
