import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de AuthorInfoPanel. La consume su `meta` y toda story que la referencie. */
export const authorInfoPanelDocs: StoryDocsEntry = { title: 'Componentes V3/AuthorInfoPanel' };
