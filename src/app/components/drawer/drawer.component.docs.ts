import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de Drawer. La consume su `meta` y toda story que la referencie. */
export const drawerDocs: StoryDocsEntry = { title: 'Componentes V3/Drawer' };
