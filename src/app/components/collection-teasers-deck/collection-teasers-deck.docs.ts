import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de CollectionTeasersDeck. La consume su `meta` y toda story que la referencie. */
export const collectionTeasersDeckDocs: StoryDocsEntry = { title: 'Componentes V3/CollectionTeasersDeck' };
