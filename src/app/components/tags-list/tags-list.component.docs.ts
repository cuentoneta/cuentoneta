import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de TagsList. La consume su `meta` y toda story que la referencie. */
export const tagsListDocs: StoryDocsEntry = { title: 'Componentes V3/TagsList' };
