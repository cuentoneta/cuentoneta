import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de AudioRecording. La consume su `meta` y toda story que la referencie. */
export const audioRecordingDocs: StoryDocsEntry = { title: 'Widgets/AudioRecording' };
