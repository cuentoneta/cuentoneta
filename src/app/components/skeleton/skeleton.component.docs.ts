import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de Skeleton. La consume su `meta` y toda story que la referencie. */
export const skeletonDocs: StoryDocsEntry = { title: 'Componentes V3/Skeleton' };
