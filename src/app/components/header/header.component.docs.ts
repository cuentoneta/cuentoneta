import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de HeaderComponent. La consume su `meta` y toda story que la referencie. */
export const headerComponentDocs: StoryDocsEntry = { title: 'HeaderComponent' };
