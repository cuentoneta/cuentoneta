import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de Resource. La consume su `meta` y toda story que la referencie. */
export const resourceDocs: StoryDocsEntry = { title: 'Componentes V3/Resource' };
