import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de HomeHero. La consume su `meta` y toda story que la referencie. */
export const homeHeroDocs: StoryDocsEntry = { title: 'Componentes V3/HomeHero' };
