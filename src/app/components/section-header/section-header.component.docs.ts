import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de SectionHeader. La consume su `meta` y toda story que la referencie. */
export const sectionHeaderDocs: StoryDocsEntry = { title: 'Componentes V3/SectionHeader' };
