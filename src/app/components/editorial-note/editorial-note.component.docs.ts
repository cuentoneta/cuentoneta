import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de EditorialNote. La consume su `meta` y toda story que la referencie. */
export const editorialNoteDocs: StoryDocsEntry = { title: 'Componentes V3/EditorialNote' };
