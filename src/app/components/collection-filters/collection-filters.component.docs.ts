import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de CollectionFilters. La consume su `meta` y toda story que la referencie. */
export const collectionFiltersDocs: StoryDocsEntry = { title: 'Componentes V3/CollectionFilters' };
