import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de CollectionTeaserCard. La consume su `meta` y toda story que la referencie. */
export const collectionTeaserCardDocs: StoryDocsEntry = { title: 'Componentes V3/CollectionTeaserCard' };
