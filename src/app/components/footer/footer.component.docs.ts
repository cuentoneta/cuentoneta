import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de FooterComponent. La consume su `meta` y toda story que la referencie. */
export const footerComponentDocs: StoryDocsEntry = { title: 'FooterComponent' };
