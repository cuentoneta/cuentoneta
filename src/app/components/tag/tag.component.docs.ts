import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de Tag. La consume su `meta` y toda story que la referencie. */
export const tagDocs: StoryDocsEntry = { title: 'Componentes V3/Tag' };
