import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de CoverImage. La consume su `meta` y toda story que la referencie. */
export const coverImageDocs: StoryDocsEntry = { title: 'Componentes V3/CoverImage' };
