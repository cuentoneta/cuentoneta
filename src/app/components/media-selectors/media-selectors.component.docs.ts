import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de MediaSelectors. La consume su `meta` y toda story que la referencie. */
export const mediaSelectorsDocs: StoryDocsEntry = { title: 'Componentes V3/MediaSelectors' };
