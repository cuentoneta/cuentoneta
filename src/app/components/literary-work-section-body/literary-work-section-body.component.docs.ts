import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de LiteraryWorkSectionBody. La consume su `meta` y toda story que la referencie. */
export const literaryWorkSectionBodyDocs: StoryDocsEntry = { title: 'Componentes V3/LiteraryWorkSectionBody' };
