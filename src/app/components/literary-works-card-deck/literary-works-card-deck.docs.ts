import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de LiteraryWorksCardDeck. La consume su `meta` y toda story que la referencie. */
export const literaryWorksCardDeckDocs: StoryDocsEntry = { title: 'Componentes V3/LiteraryWorksCardDeck' };
