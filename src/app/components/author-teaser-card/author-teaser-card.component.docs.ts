import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de AuthorTeaserCard. La consume su `meta` y toda story que la referencie. */
export const authorTeaserCardDocs: StoryDocsEntry = { title: 'Componentes V3/AuthorTeaserCard' };
