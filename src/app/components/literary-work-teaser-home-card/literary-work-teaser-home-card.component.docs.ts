import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de LiteraryWorkTeaserHomeCard. La consume su `meta` y toda story que la referencie. */
export const literaryWorkTeaserHomeCardDocs: StoryDocsEntry = { title: 'Componentes V3/LiteraryWorkTeaserHomeCard' };
