import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de CollectionInfoPanel. La consume su `meta` y toda story que la referencie. */
export const collectionInfoPanelDocs: StoryDocsEntry = { title: 'Componentes V3/CollectionInfoPanel' };
