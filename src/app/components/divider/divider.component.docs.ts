import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de Divider. La consume su `meta` y toda story que la referencie. */
export const dividerDocs: StoryDocsEntry = { title: 'Componentes V3/Divider' };
