import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de LiteraryWorkPage. La consume su `meta` y toda story que la referencie. */
export const literaryWorkPageDocs: StoryDocsEntry = { title: 'Páginas/LiteraryWorkPage' };
