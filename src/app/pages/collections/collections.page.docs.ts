import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de CollectionsPage. La consume su `meta` y toda story que la referencie. */
export const collectionsPageDocs: StoryDocsEntry = { title: 'Páginas/CollectionsPage' };
