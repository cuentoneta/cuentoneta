import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de LiteraryWorksPage. La consume su `meta` y toda story que la referencie. */
export const literaryWorksPageDocs: StoryDocsEntry = { title: 'Páginas/LiteraryWorksPage' };
