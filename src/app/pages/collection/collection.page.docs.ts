import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de CollectionPage. La consume su `meta` y toda story que la referencie. */
export const collectionPageDocs: StoryDocsEntry = { title: 'Páginas/CollectionPage' };
