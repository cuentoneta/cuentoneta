import type { StoryDocsEntry } from '@testing/storybook-docs';

/** Entrada de catálogo de AuthorPage. La consume su `meta` y toda story que la referencie. */
export const authorPageDocs: StoryDocsEntry = { title: 'Páginas/AuthorPage' };
